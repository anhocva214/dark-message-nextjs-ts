import React, { HTMLAttributes, Ref, ReactElement, RefAttributes } from 'react';
import { Plot as BasePlot } from '@antv/g2plot';
interface Options {
    [x: string]: any;
}
declare type PickedAttrs = 'className' | 'style';
export interface Plot<C extends Options> {
    new (container: HTMLElement, config: C): BasePlot<C>;
}
export interface BaseChartProps<C extends Options> extends Pick<HTMLAttributes<HTMLDivElement>, PickedAttrs> {
    /**
     * Plot Class
     * @note Internal use, should not use directly
     */
    chart: Plot<C>;
    /**
     * Plot Ref
     */
    chartRef?: Ref<BasePlot<C> | null>;
}
declare const _default: <C extends Options>(p: BaseChartProps<C> & React.RefAttributes<HTMLDivElement | null>) => ReactElement;
export default _default;
